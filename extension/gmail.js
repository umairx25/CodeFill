export async function fetchUnreadEmails(accountIndex = 0) { const response = await fetch(`https://mail.google.com/mail/u/${accountIndex}/feed/atom`, { credentials: 'include', cache: 'no-store' }); if (!response.ok) throw new Error(response.status === 401 ? 'Sign in to Gmail, then scan again.' : `Gmail returned ${response.status}.`); return parseGmailAtom(await response.text()) }
export function parseGmailAtom(xml) { const document = new DOMParser().parseFromString(xml, 'application/xml'); if (document.querySelector('parsererror')) throw new Error('Gmail returned an unreadable feed.'); return [...document.getElementsByTagName('entry')].map(entry => ({ subject: text(entry, 'title'), summary: text(entry, 'summary'), senderName: nestedText(entry, 'author', 'name'), senderEmail: nestedText(entry, 'author', 'email') })) }
function text(parent, tag) { return parent.getElementsByTagName(tag)[0]?.textContent?.trim() ?? '' } function nestedText(parent, outer, inner) { const element = parent.getElementsByTagName(outer)[0]; return element ? text(element, inner) : '' }
